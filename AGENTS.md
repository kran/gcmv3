# gcmv3 代码约束

## 硬性规则（违反即重写）

### 1. 不在 if 里用长语句
绝不写 `if err := 很长(...); err != nil {` 这种长 if 头。
拆成变量声明 + 独立条件：

```go
// ❌ 不要
if _, err := s.db.Insert("auth_methods", x).Exec(); err != nil {
    return err
}

// ✅ 要
_, err := s.db.Insert("auth_methods", x).Exec()
if err != nil {
    return err
}
```

### 2. 讨论优先，不擅自改
`为什么 X？` / `你觉得呢？` / `先讨论`——是**问询/讨论**，不是让我改。
先回答/讨论清楚，用户点头**才动手**。

### 3. 结构/接口/表变更先对齐再实现
不 diff 大改动不做确认就重构。

### 4. 零消费方的 API 不进对外契约
往接口上加方法之前，先拿出调用点。**"以后可能要用"不是理由**
（gcmv2 的教训：38 个方法里 13 个没有任何消费方）。

## 边界（本项目的立项原因）

`core` 只保留**六件事**：

| 概念 | 内容 |
|---|---|
| `auth` | opaque 凭据 + realm 绑定会话 |
| `node` | 节点 CRUD 与读投影 |
| `edge` | 引用边与图原语 |
| `hook` | 生命周期事件总线 |
| `schema` | 类型系统（由 `types` 包承载） |
| `query` | 结构化查询（AST → SQL） |

**不进 core** 的东西（gcmv2 里长出来的，逐条清理）：
- `search`：全文检索是**插件**（可选、可卸）
- `tree`：树视图是**展示**（内存结构 + 渲染，属于 web）
- `merge`：只有预览不能执行的半成品 —— 不进
- `settings`：通用 KV 仓储，属于承载它的那层
- `policy`：行范围/字段掩码是**授权词汇**，属于 web

判据：**能卸掉且不影响内核 CRUD 的 ⇒ 插件；只给界面看的 ⇒ web；只有内核读写要它 ⇒ core。**

## 偏好

- 简单直接、最低魔法（fail-loud 哲学）
- 新类型同类放一起、独立文件（每 kind 一个文件）
- 用 cast 容错取值（字段类型多变——不裸断言）
- 一个概念一件事：**导出面尽量小**，内部函数不做"预留的替身"
  （gcmv2 的教训：`xxxTx` 私有方法零复用、9 个函数的 expand）

## 验证约定（踩出来的，违反过）

- **"绿"只能由退出码定义**：`go test ./... >/dev/null 2>&1; echo $?`。不要用
  `grep -c '--- FAIL'` 之类去数日志行 —— **编译失败**只打印 `FAIL pkg [build failed]`，
  不含 `--- FAIL`，会被当成通过。
- 闸门同理：`a && b && c; echo $?`。
- **脚本改代码必须 assert**：替换前 `assert old in s`，替换后确认新文本存在。
  静默 no-op 配上 `print("✓")` 会让后面几步建立在假象上。
- **真机核对不可省**：单测夹具数据干净，有些 bug 只有真启动才现形。
- **破坏性验证前先提交**，或不用 `git checkout --` 恢复未提交的修复。
